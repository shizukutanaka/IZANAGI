#!/bin/sh
# Pre-push: the full verification gate — one definition of "green", shared
# with CI (docs/ci/ci.yml).
# Enable from the repository root with:  git config core.hooksPath .githooks
set -eu
cd "$(git rev-parse --show-toplevel)"
exec tools/gate.sh
